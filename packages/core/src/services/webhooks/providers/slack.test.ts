// import { Service as SlackService } from './slack';
// import { NodesModel } from '../../../models';
// import { connect, disconnect } from '../../../db';

test.skip('Slack Test', async () => {
  // const slackService = new SlackService();
  // await connect();
  // const testNode = await NodesModel.findOne({ backend: 'algtestnet' })
  //   .populate('chain')
  //   .populate({ path: 'host', populate: 'location' })
  //   .exec();
  // await slackService.addWebhookForNode(testNode);
  // await disconnect();
});
